#!/bin/env ruby
require 'date'
require_relative '../lib/done_log.rb'

options = DoneLogOptions.parse!(ARGV)

case options[:action]
when :edit
  DoneLog.edit_logs(options[:time_period])
when :show
  DoneLog.show_logs(options[:time_period])
else
  abort "Need to either --edit or --show logs"
end
